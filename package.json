{
  "name": "node-starter",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx rollup input.js -f esm -o bundle.js"
  },
  "devDependencies": {
    "rollup": "^4.2.0"
  }
}
